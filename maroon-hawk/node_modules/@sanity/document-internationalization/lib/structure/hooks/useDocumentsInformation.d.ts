import type { SanityDocument } from '@sanity/client';
import React from 'react';
export declare const useDocumentsInformation: (schema: string) => {
    pending: boolean;
    setPending: React.Dispatch<React.SetStateAction<boolean>>;
    documents: SanityDocument<Record<string, any>>[];
    baseDocuments: SanityDocument<Record<string, any>>[];
    translatedDocuments: SanityDocument<Record<string, any>>[];
    idStructureMismatchDocuments: SanityDocument<Record<string, any>>[];
    documentsSummaryInformation: {
        idStructureMismatch: SanityDocument<Record<string, any>>[];
        missingLanguageField: SanityDocument<Record<string, any>>[];
        missingDocumentRefs: SanityDocument<Record<string, any>>[];
        missingBaseDocumentRefs: SanityDocument<Record<string, any>>[];
        orphanDocuments: SanityDocument<Record<string, any>>[];
        referenceBehaviorMismatch: SanityDocument<Record<string, any>>[];
        baseLanguageMismatch: SanityDocument<Record<string, any>>[];
    };
    fetchInformation: (selectedSchema: string) => Promise<void>;
};
